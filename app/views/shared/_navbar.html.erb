<% if request.env['PATH_INFO'] != "/" && request.env['PATH_INFO'] != "/home" && request.env['PATH_INFO'] != "/theme" && request.env['PATH_INFO'] != "/" && request.env['PATH_INFO'] != "/users/sign_in" %>
  <div class="navbar-wagon" style="background: #3A475B;">
    <!-- Logo -->
    <%= link_to home_path, class: "navbar-wagon-brand" do %>
      <%= image_tag "logo-background.png", size: "117x42" %>
    <% end %>

    <!-- Right Navigation -->
    <div class="navbar-wagon-right hidden-xs hidden-sm">

      <% if user_signed_in? %>

        <!-- Links when logged in -->
        <div class="navbar-items">
          <div class="shopping-bag-container">
          <%= link_to order_show_url(current_user.cart.id), method: :get, class: "navbar-wagon-item navbar-wagon-link" do %>
            <%= image_tag "shoppingbag.png", size: "62x30", style: "position: absolute;" %>
            <div class="text-block"><%= OrderOutfit.where(order_id: current_user.cart.id).count %></div>
          <% end %>
          </div>

          <%= link_to destroy_user_session_path, method: :delete do %>
              <i class="fa fa-sign-out" style="margin-left: 100px;"></i>  <%= t(".sign_out", default: "Log out") %>
          <% end %>
        </div>
      <% else %>
        <!-- Login link (when logged out) -->

      <% end %>
    </div>

    <!-- Dropdown list appearing on mobile only -->
    <!-- <div class="navbar-wagon-item hidden-md hidden-lg">
      <div class="dropdown">
        <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
        <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
          <li><a href="#">Some mobile link</a></li>
          <li><a href="#">Other one</a></li>
          <li><a href="#">Other one</a></li>
        </ul>
      </div>
    </div> -->
  </div>
<% else %>
  <div class="navbar-wagon">
      <!-- Logo -->
      <%= link_to home_path, class: "navbar-wagon-brand" do %>
        <%= image_tag "logo-background.png", size: "117x40" %>
      <% end %>

      <!-- Right Navigation -->
      <div class="navbar-wagon-right hidden-xs hidden-sm">

        <% if user_signed_in? %>

          <!-- Links when logged in -->
          <%= link_to order_show_url(current_user.cart.id), method: :get, class: "navbar-wagon-item navbar-wagon-link" do %>
            <%= image_tag "shoppingbag.png", size: "62x30" %>
          <% end %>

          <%= link_to destroy_user_session_path, method: :delete do %>
              <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
          <% end %>

        <% else %>
          <!-- Login link (when logged out) -->


        <button type="button" class="btn btn-outline-light" data-toggle="modal" data-target="#exampleModal">
          Login
        </button>

        <% end %>
      </div>

      <!-- Dropdown list appearing on mobile only -->
      <!-- <div class="navbar-wagon-item hidden-md hidden-lg">
        <div class="dropdown">
          <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li><a href="#">Some mobile link</a></li>
            <li><a href="#">Other one</a></li>
            <li><a href="#">Other one</a></li>
          </ul>
        </div>
      </div> -->
    </div>


          </div>
        </div>
<% end %>




